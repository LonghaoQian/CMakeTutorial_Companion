set(MyLibA_INCLUDE_DIR ${CMAKE_CURRENT_LIST_DIR} PARENT_SCOPE)

if (${CMAKE_BUILD_TYPE} STREQUAL "Debug")
    set(MyLibA_LINK_DIR ${CMAKE_CURRENT_LIST_DIR}/debug PARENT_SCOPE)
else()
    set(MyLibA_LINK_DIR ${CMAKE_CURRENT_LIST_DIR}/release PARENT_SCOPE)
endif()